apiVersion: v1
kind: Service
metadata:
  name: {{ include "ranchertest.fullname" . }}
  labels:
    {{- include "ranchertest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ranchertest.selectorLabels" . | nindent 4 }}
