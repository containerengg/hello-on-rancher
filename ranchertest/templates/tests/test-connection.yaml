apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ranchertest.fullname" . }}-test-connection"
  labels:
    {{- include "ranchertest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ranchertest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
